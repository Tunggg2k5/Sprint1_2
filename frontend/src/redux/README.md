Shared client state will live here when the app needs Redux-level stores.

Current screens still use local React state and context, so this folder is kept
as the architecture placeholder described in fullstack.md.
